"""配置中心：所有阈值与常量集中管理，禁止硬编码。

调整下方常量即可改变复盘行为，无需改动业务代码。
"""
from __future__ import annotations

# ============================================================================
# 输入列规范（与招商证券导出列头一致）
# ============================================================================
# 必需列：缺失任一列将明确报错（如"缺少关键列：佣金"）
REQUIRED_COLUMNS: list[str] = [
    "证券代码",
    "证券名称",
    "成交日期",
    "成交价格",
    "成交数量",
    "发生金额",
    "剩余数量",
    "佣金",
    "印花税",
]

# 可选规费列：缺失时按 0 计入规费合计（部分券商导出可能省略）
OPTIONAL_FEE_COLUMNS: list[str] = ["经手费", "证管费", "过户费"]

# ============================================================================
# 数据清洗
# ============================================================================
# 现金理财/产品申赎记录关键词，命中即剔除（仅保留股票/ETF二级市场买卖）
CASH_PRODUCT_KEYWORDS: list[str] = ["天添利", "产品申购", "产品赎回"]

# ============================================================================
# 成本分析
# ============================================================================
# 佣金率预警阈值（单位：万分之一 ‰‱），超过即输出红色警告
COMMISSION_RATE_WARN_BPS: float = 5.0
# 单笔佣金最高前 N 笔
TOP_N_HIGH_COMMISSION: int = 5

# ============================================================================
# 绩效复盘
# ============================================================================
# 盈利贡献榜数量
TOP_N_PROFIT: int = 3
# 亏损拖累榜数量
TOP_N_LOSS: int = 3

# ============================================================================
# 仓位管理
# ============================================================================
# 前三大重仓标的
TOP_N_HOLDINGS: int = 3

# ============================================================================
# 输出
# ============================================================================
# 落盘文件名前缀（最终文件：fupan_{date}.txt）
OUTPUT_FILENAME_PREFIX: str = "fupan_"
