"""提示词模板：构建发给 DeepSeek 视觉模型的分析提示词。

将用户输入的盘中模拟盘口数据（现价/量比/量比倍数）动态填入提示词，
要求模型扮演资深量化交易策略分析师，严格按照结构/动量/赔率三要素
及突破判定标准推导开仓概率，并只返回标准 JSON 对象。
"""
from __future__ import annotations


def build_prompt(price_now: float, volume_ratio: float, vol_multiplier: float) -> str:
    """返回完整提示词文本。

    参数
    ----
    price_now : 当前现价
    volume_ratio : 预估量比
    vol_multiplier : 预估全天成交量 / 昨日成交量

    返回
    ----
    str : 完整提示词，模型应只返回 JSON 对象
    """
    return f"""你是一位极其严谨、客观的量化交易策略分析师。请严格按照以下限制条件和逻辑框架分析提供的股票图表（中京电子 002579），并只返回标准的 JSON 对象，严禁输出任何额外的解释性文字或 Markdown 代码块。

一、 强制输入的模拟数据（当前盘口假设）：
用户将会提供今天盘中的预期量价数据，请根据这些数据进行推演：
- 当前现价 (price_now) = {price_now}
- 预估量比 (volume_ratio) = {volume_ratio}
- 预估全天成交量/昨日成交量 (vol_multiplier) = {vol_multiplier}

二、 严格分析的限定条件：
1. 必须拆解图中的三个核心要素：
   - 结构 (Structure)：分析 MA5、MA20、MA60 的排列状态。特别关注当前价格 ({price_now}) 与 MA60 的关系。图中标注"未通过：A"，这意味着结构尚未突破（大概率受压于MA60）。
   - 动量 (Momentum)：结合 MACD（金叉后红柱是否放大）和 RSI（52.1中性偏多，是否有效突破60）评估动能。
   - 赔率 (Odds)：参考图中标注的止损 14.48、止盈 17.57，赔率为 2.00:1，此为静态数据。
2. 突破判定标准（程序化具体条件）：
   - 结构突破条件：收盘价必须 > 16.00（有效突破MA60），且 MA5 > MA20。
   - 量能配合条件：量比必须 > 1.5（放量突破），缩量突破视为假突破。
   - 动量确认条件：MACD红柱必须比前一日更长，RSI 处于 50-70 之间的强势区。
3. 概率推导逻辑：
   - 只有当模拟输入数据完全满足以上全部条件时，才给予较高的上涨概率评估（可参考历史形态胜率 65%-70%）。
   - 如果量比 < 1.0 或价格未站稳 16.00，必须给出较低的上涨概率（约 30%-40%），并明确提示冲高回落风险。

三、 输出格式（JSON Schema）：
请严格按照以下 JSON 格式输出，不要有逗号错误：
{{
    "conditions": {{
        "price_now": "用户输入的现价",
        "volume_ratio": "用户输入的量比",
        "passes_structure_A": "布尔值 (true/false，是否满足放量突破MA60的结构条件)"
    }},
    "probability": "浮点数 (0到1之间，表示根据上述条件判定后的上涨概率)",
    "hold_conditions": ["条件1", "条件2", "条件3"],
    "main_reason": "详细的分析理由（基于均线、MACD、RSI和量价关系）",
    "risk_alert": "明确的潜在风险提示（如：无量突破假反弹，警惕跌破14.48止损线）"
}}"""
