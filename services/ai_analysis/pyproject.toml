[project]
name = "trading-lab-ai-analysis"
version = "0.1.0"
description = "AI 截图分析模块（DeepSeek 视觉模型多模态分析）"
requires-python = ">=3.11"
# 依赖面文档；实际安装由根 pyproject.toml 统一声明并装入共享 .venv
dependencies = [
    "requests>=2.31",
]

[tool.uv]
package = false
