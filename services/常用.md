/macro-data-service 
使用场景：获取中国及美国宏观指标数据。

/macro-charts
使用场景：生成中国及美国宏观指标的 dashboard 图。
使用方法：./scripts/refresh_macro_charts.sh

/sell-monitor
使用场景：监控股票持仓，自动卖出。
uv run services/sell_monitor/main.py --symbol 600550 --date 2026-08-12  

/calc_indicators
使用场景：计算股票入场指标。
uv run python services/calc_indicators/main.py 000837 -p aggressive_short -r -v 
uv run python services/calc_indicators/main.py 300602 -p ultra_short -r

/crawler_news

uv run python services/crawler_news/main.py crawl --date 2026-08-17
uv run python services/crawler_news/main.py report --start-date 2026-08-01 --end-date 2026-08-17 --format html

uv run python services/crawler_news/main.py crawl --symbol 600519 --date 2026-08-17

/bankuai-service
# 默认：前3个概念板块，default 配置
./scripts/scan_leaders_chart.sh

# 传入 aggressive_short 配置档
./scripts/scan_leaders_chart.sh -p aggressive_short
./scripts/scan_leaders_chart.sh -p ultra_short

# 叠加配置档 + 自动打开图片
./scripts/scan_leaders_chart.sh -p aggressive_short -p long_term --open

# 前5个行业板块
./scripts/scan_leaders_chart.sh --top-n 5 --plate-type 行业

# 某只股票在超短线情况下是否适合入场
uv run python services/calc_indicators/main.py 603330 -p ultra_short -r --open    

# 板块轮动和市场趋势
# ✅ 推荐：收盘后默认全流程（预警+ADX+轮动 → Markdown+CSV落盘）
uv run services/alarming_monitor/main.py

# 只看轮动（不跑预警、不写文件）
uv run services/alarming_monitor/main.py --rotation-only --no-csv --no-report

# 回测某日（默认昨日）
uv run services/alarming_monitor/main.py --date 2026-08-15

# 查看近3个月预警红灯历史
uv run services/alarming_monitor/main.py --history

# 跑完整单元测试
uv run pytest services/alarming_monitor/test_monitor.py -v
# 科创综指档（主指数=科创50 sh000688，BREADTH_INDEX + KDJ_INDEX 同步切换），指的是S7指标
uv run services/alarming_monitor/main.py --profile kc
uv run services/alarming_monitor/main.py --profile kechuang   # 长别名
# 组合示例：科创综指档 + 只跑 S6+S7（资金面+中长周期形态）
uv run services/alarming_monitor/main.py --profile kc --indicator margin,kdj
# 组合示例：科创综指档 + 跳过连板（纯预警+轮动）
uv run services/alarming_monitor/main.py --profile kc --no-linkban


# 每日复盘
# 从行业板块里挑选符合三个条件的股票，指定日期是今天，缺省是昨天，如果早上运行，不需要date参数，如果晚上运行，加上date今日参数
uv run services/bankuai-service/main.py --plate-type 行业 --date 2026-08-26

# 缺省是概念板块，列出板块前十和后十，以及三级龙头股票
uv run services/bankuai-service/main.py --plate-type 行业

# 从某个板块里挑选符合三个条件的股票
./scripts/scan_leaders_chart.sh --sectors 电力
./scripts/scan_leaders_chart.sh -p ultra_short 

# 获取某只股票的研报简报
uv run services/yanbao-info/main.py --symbol 000721

# 获取某只股票的入场条件状况
uv run python services/calc_indicators/main.py 600396 -p ultra_short -r --open

# 获取市场整体情况以及轮动板块情况，默认全跑 S1~S6 + 轮动 + 连板
uv run services/alarming_monitor/main.py --date 2026-08-27
# 获取连板情况
uv run services/alarming_monitor/main.py --linkban-only 
# 获取两融杠杆情况
uv run services/alarming_monitor/main.py --indicator margin --no-rotation --no-linkban
# 跑 S1~S5 但不跑 S6（两融数据拉取慢 / 不想查接口时）
uv run services/alarming_monitor/main.py --no-margin
# 只跑 S1 + S6
uv run services/alarming_monitor/main.py --indicator turnover,margin --no-rotation --no-linkban
# 仅 S6 + 写回 CSV + 静默
uv run services/alarming_monitor/main.py --indicator margin --no-rotation --no-linkban --quiet
