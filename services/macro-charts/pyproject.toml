[project]
name = "trading-lab-macro-charts"
version = "0.1.0"
description = "宏观数据可视化图表模块（Plotly，与采集模块解耦）"
requires-python = ">=3.11"
# 依赖面文档；实际安装由根 pyproject.toml 统一声明并装入共享 .venv
dependencies = [
    "plotly>=5.20",
    "pandas>=2.0",
    "numpy>=1.26",
]

[tool.uv]
package = false
